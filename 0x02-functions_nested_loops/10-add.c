#include "main.h"

/**
 * add - adds two integers
 * @c: first number
 * @y: second number
 * Return: the addition of both
 */
int add(int c, int y)
{
	int result;

	result = c + y;
	return (result);
}
